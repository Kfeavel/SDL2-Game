{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Execute",
            "type": "shell",
            "command": "${workspaceFolder}/bin/game_exe",
            "options": {
                "cwd": "${workspaceFolder}/bin"
            },
            "problemMatcher": [],
            "dependsOn": [
                "CMake: build"
            ],
        },
        {
            "type": "cmake",
            "label": "CMake: build",
            "command": "build",
            "targets": [
                "all"
            ],
            "group": "build",
            "problemMatcher": [],
            "detail": "CMake template build task"
        }
    ]
}
